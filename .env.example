# =================================================================
# ==                     ENVIRONMENT VARIABLES                   ==
# =================================================================
#
# - Copy this file to .env and fill in your secrets.
# - The .env file is git-ignored and should NOT be committed.
# - This file is used by Docker Compose to configure all services.

# -- App Environment --
# Set to 'production' for production builds, otherwise 'development'
NODE_ENV=development

# -- Timezone --
# Sets the timezone for services like cron jobs inside the containers.
# Example: America/New_York, Europe/London
TZ=America/Chicago

# =================================================================
# ==                       DATABASE (NEON)                       ==
# =================================================================
#
# --- Neon Database URLs ---
# You can get these from your Neon project dashboard.
# IMPORTANT: Both URLs MUST include the '?sslmode=require' parameter.
#
# DATABASE_URL: The direct connection URL. Used for running Drizzle migrations.
# Example: postgres://<user>:<password>@ep-xxxx.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=
#
# DATABASE_POOL_URL: The pooled connection URL. Used by the backend application for queries.
# This is recommended for web applications to manage connections efficiently.
# Example: postgres://<user>:<password>@ep-xxxx-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_POOL_URL=

# =================================================================
# ==                        AUTHENTICATION                       ==
# =================================================================
#
# A strong, random secret for signing JWTs.
JWT_SECRET=changeme

# =================================================================
# ==                      FRONTEND (VITE)                        ==
# =================================================================
#
# The URL for the backend API, which the Vite frontend will call.
# This should match the backend service's port.
VITE_API_URL=http://localhost:3000
